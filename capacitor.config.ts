import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.romebot.org',
  appName: 'Romebot',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
